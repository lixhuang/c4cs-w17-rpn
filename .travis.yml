language: python
python:
  - "3.5"
# command to run tests
#before-install:

install: 
  - pip install --upgrade termcolor
  - pip install coveralls
script: 
  - coveralls run unittest
  
after_sucess:
  - coveralls

